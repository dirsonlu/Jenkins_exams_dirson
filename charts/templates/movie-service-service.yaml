apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30007
      protocol: TCP
  selector:
    app: movie-service
